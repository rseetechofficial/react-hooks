
const Greet = (props) => {
    return (
        <>
        {/* <h1>{props.name} from {props.location} </h1>
        <h3>{props.children}</h3> */}
        <h4>From Greeting</h4>
        </>
        
    )
}

export default Greet;